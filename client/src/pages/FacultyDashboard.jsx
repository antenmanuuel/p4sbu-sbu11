import React from "react";
import StudentDashboard from "./StudentDashboard";

const FacultyDashboard = ({ darkMode }) => {
  return <StudentDashboard darkMode={darkMode} />;
};

export default FacultyDashboard;

